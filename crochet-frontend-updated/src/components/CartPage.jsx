import { Container, Heading } from "@chakra-ui/react";

export default function CartPage() {
    return (
        <Container padding={40}>
            <Heading>This is the cart Page!</Heading>
        </Container>
    )
}